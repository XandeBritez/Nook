<Window x:Class="Nook.ClipboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nook — Área de transferência" Width="400" Height="500"
        MinWidth="320" MinHeight="360" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Buscar" FontWeight="Bold"/>
        <TextBox x:Name="SearchBox" Grid.Row="1" Margin="0,2,0,8"
                 TextChanged="SearchBox_TextChanged"/>

        <ListBox x:Name="ClipList" Grid.Row="2"
                 MouseDoubleClick="ClipList_DoubleClick"
                 KeyDown="ClipList_KeyDown">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="2">
                        <TextBlock Text="{Binding Preview}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding Time}" FontSize="10" Opacity="0.6"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Copiar" Width="80" Padding="6,4" Margin="0,0,6,0"
                    Click="CopyButton_Click"/>
            <Button Content="Apagar" Width="80" Padding="6,4" Margin="0,0,6,0"
                    Click="DeleteButton_Click"/>
            <Button Content="Limpar tudo" Width="90" Padding="6,4"
                    Click="ClearButton_Click"/>
        </StackPanel>

        <DockPanel Grid.Row="4" Margin="0,8,0,0">
            <CheckBox x:Name="PersistCheck" Content="Manter ao sair"
                      VerticalAlignment="Center"
                      Checked="PersistCheck_Changed" Unchecked="PersistCheck_Changed"/>
            <TextBlock x:Name="StatusText" Opacity="0.7" FontSize="11"
                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</Window>
